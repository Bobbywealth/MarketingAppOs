############################
# MarketingOS (Example Env) #
############################

# --- Core ---
NODE_ENV=development
PORT=5001
SESSION_SECRET=change-me

# --- App URL (used in email templates / links) ---
APP_URL=http://localhost:5001

# --- Twilio (SMS/MMS) ---
# Required for outbound SMS (server/twilioService.ts)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+15555555555
TWILIO_WHATSAPP_NUMBER=+15555555555 # Separate WhatsApp sender if different from SMS number
#
# Optional: use a Twilio Messaging Service (recommended for higher volume / easier sender management)
# If set, the server will use messagingServiceSid instead of TWILIO_PHONE_NUMBER.
TWILIO_MESSAGING_SERVICE_SID=

# SMS recipient for system-wide alert notifications (task deadlines, etc.)
NOTIFICATION_ALERT_SMS_TO=+18089139158
#
# --- Booking SMS confirmations (public /api/bookings) ---
# Send a transactional SMS to the person who books a strategy session.
BOOKING_SMS_ENABLED=false
# Optional template with placeholders:
# {name} {whenEt} {phone} {email} {company} {eventId}
BOOKING_SMS_TEMPLATE=
#
# --- Booking email confirmations (public /api/bookings) ---
# Sends an email confirmation to the booker and an alert email to admins (if SMTP is configured).
BOOKING_EMAIL_ENABLED=true
#
# Optional but recommended for inbound webhook signature validation:
# Set to your public base URL (no trailing slash). Example:
# TWILIO_WEBHOOK_BASE_URL=https://www.marketingteam.app
TWILIO_WEBHOOK_BASE_URL=

# --- Email Verification Enforcement (optional) ---
# When set to true, API routes protected by isAuthenticated will require emailVerified=true
# for non-exempt roles (admins/managers are exempt).
ENFORCE_EMAIL_VERIFICATION=false

# --- SMTP (required for notification emails) ---
# See SMTP_SETUP.md
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username-or-email
SMTP_PASS=your-smtp-password-or-api-key

# Optional but recommended
SMTP_FROM_NAME="Marketing Team"
SMTP_FROM_EMAIL=notifications@yourdomain.com
SMTP_REPLY_TO=support@yourdomain.com
# SMTP_TLS_REJECT_UNAUTHORIZED=false


# --- Frontend (Vite / native wrapper) ---
# When building the app inside a native WebView (Capacitor), relative `/api/*` calls
# won’t reach your hosted backend unless a base URL is provided.
# Example:
VITE_API_BASE_URL=https://www.marketingteam.app
