import fetch from 'node-fetch';

async function testApi() {
  const cookie = 'connect.sid=s%3A...'; // I don't have a valid session cookie
  // But wait, I can just use the database to bypass auth if I run it on the server
}

